services:
  - type: web
    name: hashstock-helper
    plan: free
    runtime: node
    buildCommand: npm install
    startCommand: node local-helper.cjs
    envVars:
      - key: PORT
        value: 3001
